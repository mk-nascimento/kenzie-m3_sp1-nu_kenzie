import "./style.css";

function Form({ listTransactions, setListTransactions }) {
  return (
    <form>
      <input type="text" />
      <input type="text" />
      <select name="" id="">
        <option value="" selected disabled></option>
      </select>
    </form>
  );
}
